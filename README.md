# Сериализатор двусвязного списка

Программа для сериализации и десериализации двусвязного списка с произвольными ссылками.

# Сборка
cmake . -S build
cmake --build build

# Запуск
cd build
./list_serializer ../inlet.in
